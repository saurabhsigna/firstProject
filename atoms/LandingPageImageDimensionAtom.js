import { atom } from "recoil";

export const imageWidthState = atom({
  key: "imageWidthState",
  default: null,
});

export const imageHeightState = atom({
  key: "imageHeightState",
  default: null,
});
